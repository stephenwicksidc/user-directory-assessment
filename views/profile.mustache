<!DOCTYPE =html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" type="text/css" href="/styles.css">
    </head>
    <body>
        <h1>Robots User Directory</h1>
        <div class="directoryContainer">
            <div class="usercard">
            {{#users.0}}
                <img src="{{avatar}}"><br>
                <p class="nameOfPerson">{{name}}</p>
                <p class="jobInfo">{{job}}, {{company}}</p>
            </div>
            {{/users.0}}
        </div>
    </body>
<html>